To set up this, make this the current working directory and do the 
//to create the docker image
1.docker build -t data-generator . 
//creates the container with a sharedvolume
2.docker run -d --name data-generator -v shared_volume:/svolume data-generator //or -itd
